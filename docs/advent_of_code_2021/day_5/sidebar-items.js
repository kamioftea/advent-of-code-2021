initSidebarItems({"fn":[["get_axial_intersections","Filter out diagonal lines before running the remaining lines through [`get_intersections`]"],["get_intersections","For each line, iterate over it’s points adding each to a set (visited at least once). If that insert fails, we’ve already seen that point so add it to a second set (visited at least twice). Points repeated more than twice can be ignored, as this is not needed to provide the puzzle solution. Return that set, the length of the set will give the number of points where two of more lines intersect."],["parse_input","Takes a string with lines in the form `(x1,y1) -> (x2,y2)` and converts it into a list of [`Line`]s. Parsed using a regular expression."],["run","The entry point for running the solutions with the ‘real’ puzzle input."]],"struct":[["Line","Represent a line using the co-ordinates of each end."]]});