<!doctype html>
<html lang="en">
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<link rel="stylesheet" href="{{ '/root.css' | url}}">
	<title>{{ title }}</title>
</head>
<body>
	<header class="cover-image">
		<div class="title-wrapper">
		<h1>{{ header }}</h1>
		</div>
	</header>
	<section class="main">
	  {{ content | safe }}
	</section>
</body>
</html>
